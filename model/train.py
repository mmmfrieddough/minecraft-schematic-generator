# Assuming you have a device defined
import torch

from .model import model


def train(num_epochs: int):
    

        # Save checkpoint if this is the best model so far
        # ...

    # Remember to move tensors to the device (GPU or CPU) where the model is
